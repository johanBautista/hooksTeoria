import React from 'react';

const Productos = ({ productos }) => {
  console.log(productos);

  return (
    <div>
      <p>desde productos-component</p>
    </div>
  );
};

export default Productos;
